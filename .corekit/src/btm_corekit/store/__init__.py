"""What persists: the clock that stamps it, the filesystem effects that
write it, the identifiers that name it, and the three shapes a skill keeps
(an append-only event log, a free pad, and the session directory holding
both).
"""
