"""How a run says what happened: the failure types, the two output channels,
the corrective order a rejection carries, and the guards that raise one.

Nothing here knows what a record is. Everything else may depend on it.
"""
